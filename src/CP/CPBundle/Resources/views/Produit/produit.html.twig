{% extends "::base.html.twig" %}

{% block body %}

    <div class="row">
        <div class="box">
            <div class="col-md-6">
                <img width="470" height="310" class="img-responsive" src="{{ asset('uploads/' ~ produit.image.path) }}">
            </div>
            <div class="col-md-6">
                <h2>{{ produit.name }}</h2>
                <p>{{ produit.description|raw }}</p>

                <div class="row">
                    <div class="col-md-6">
                        <h3>{{ produit.price }}€   </h3>

                        <h3>  <form name="_xclick" action="https://www.paypal.com/cgi-bin/webscr" method="post">
                                <input type="hidden" name="cmd" value="_xclick">
                                <input type="hidden" name="business" value="clanmanagerohmyclan@gmail.com">
                                <input type="hidden" name="currency_code" value="EUR">
                                <input type="hidden" name="item_name" value="{{ produit.name }}">
                                <input type="hidden" name="amount" value="{{ produit.price }}">
                                <input type="image" src="http://www.paypalobjects.com/fr_XC/i/btn/btn_buynow_LG.gif" border="0" name="submit" alt="Make payments with PayPal - it's fast, free and secure!">
                            </form></h3>

                    </div>
                    <div class="col-md-6">
                        <a href="{{ path('cp_produits') }}" class="btn btn-default pull-right" role="button">Retour</a>
                    </div>
                </div>
            </div>
        </div>
    </div>



{% endblock %}