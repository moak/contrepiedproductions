{% extends "::base_admin.html.twig" %}

{% block body %}

    <div class="row">
        <div class="col-lg-12">
            <h1 class="page-header">Liste des images spectacle</h1>
        </div>
        <!-- /.col-lg-12 -->
    </div>



    <ol class="breadcrumb">
        <li><a href="{{ path('admin') }}"><p class="fa fa-home"></p> Dashboard</a></li>
        <li class="active">Images spectacle</li>
    </ol>


    <!-- Page Content -->
    <div class="container">
        <a href="{{ path('admin_spectacleimage_add')}}"><button class="btn btn-default btn-success">Ajouter une image spectacle</button></a>
        <hr>
        {# form_start(form, {'action': path('coc_homepage'), 'method': 'GET'}) }} #}

        <table class="table">
            <thead>
                <tr>
                    <th>Spectacle</th>
                    <th>Nom</th>
                    <th></th>
                </tr>
            </thead>
            <tbody>

            {% if spectacleimages|length != 0 %}
                {% for spectacleimage in spectacleimages %}
                    <tr>
                        <td>{{ spectacleimage.spectacle.title }}</td>
                        <td><img height="150" width="400" src="{{ asset('uploads/' ~ spectacleimage.path) }}"></td>
                        <td>
                            <a href="{{ path('admin_spectacleimage_edit', { 'id': spectacleimage.id }) }}" class="btn btn-default btn-info">Editer</a>
                            <a href="{{ path('admin_spectacleimage_delete', { 'id': spectacleimage.id }) }}" class="btn btn-default btn-danger">Suppprimer</a>
                        </td>
                    </tr>
                {% endfor %}
            {% else %}
                <tr>
                    <td>-</td>
                    <td>0 spectacle spectacle trouvé</td>
                    <td>-</td>

                    <td></td>
                </tr>
            {% endif %}
            </tbody>
        </table>
        <hr>


    </div>
    <!-- /.container -->

{% endblock %}

{% block javascripts %}
    <script src="{{ asset('js/footable.js') }}" type="text/javascript"></script>
{% endblock %}